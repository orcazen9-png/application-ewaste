name: Publish native accounts demo APK
on:
  push:
    branches: [codex/erp-release]
  workflow_dispatch:
permissions:
  contents: write
  actions: read
jobs:
  release:
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[publish-apk]')
    runs-on: ubuntu-latest
    timeout-minutes: 40
    env:
      API_ORIGIN: https://ewaste-accounts-demo.ewaste-marketplace.workers.dev
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
      - uses: android-actions/setup-android@v4
      - run: npm ci && npm test && npm run check && npm run build
      - name: Load the permanent signing identity
        env:
          ANDROID_SIGNING_JSON: ${{ secrets.ANDROID_SIGNING_JSON }}
        run: python3 scripts/prepare-android-signing.py
      - name: Download the actual previously distributed v7 APK
        uses: actions/download-artifact@v4
        with:
          github-token: ${{ github.token }}
          repository: orcazen9-png/application-ewaste
          run-id: 35951746122
          name: E-Waste-Marketplace-native-v7
          path: previous-release
      - name: Build the signed native release and test APK
        working-directory: native-android
        run: |
          set -euo pipefail
          bash ./gradlew assembleRelease -PaccountFoundation=true -PinvitationSignIn=true -PaccountApiOrigin="$API_ORIGIN" -PappVersionCode=8 -PappVersionName=3.4-native-facilities --no-daemon
          bash ./gradlew assembleDebug assembleDebugAndroidTest -PaccountFoundation=true -PinvitationSignIn=false -PaccountApiOrigin="$API_ORIGIN" -PappVersionCode=8 --no-daemon
      - name: Verify release certificate, package and native implementation
        run: |
          set -euo pipefail
          sdktools="$(find "$ANDROID_HOME/build-tools" -mindepth 1 -maxdepth 1 -type d | sort -V | tail -1)"
          mkdir -p apk-download upgrade-baseline
          apk=native-android/app/build/outputs/apk/release/app-release.apk
          cp previous-release/E-Waste-Marketplace-native-v7.apk upgrade-baseline/app.apk
          cp native-android/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk upgrade-baseline/tests.apk
          "$sdktools/apksigner" verify --verbose --print-certs "$apk" > apk-download/verification.txt
          "$sdktools/aapt" dump badging "$apk" >> apk-download/verification.txt
          "$sdktools/apksigner" verify --print-certs upgrade-baseline/app.apk > upgrade-baseline/certificate.txt
          grep "package: name='com.ewaste.marketplace.collector' versionCode='8' versionName='3.4-native-facilities'" apk-download/verification.txt
          python3 - <<'PY'
          import pathlib,re
          expected=pathlib.Path('native-android/signing-certificate.sha256').read_text().strip().lower()
          for file in ['apk-download/verification.txt','upgrade-baseline/certificate.txt']:
              hashes=re.findall(r'certificate SHA-256 digest:\s*([a-f0-9]{64})',pathlib.Path(file).read_text(),re.I)
              assert hashes and all(h.lower()==expected for h in hashes), 'Signing certificate changed'
          PY
          if grep -E 'application-testOnly|application-debuggable' apk-download/verification.txt; then exit 1; fi
          if grep -R -E 'import android.webkit|com.getcapacitor' native-android/app/src/main; then exit 1; fi
          cp "$apk" apk-download/E-Waste-Marketplace-native-v8.apk
          sha256sum apk-download/*.apk >> apk-download/verification.txt
      - name: Enable emulator acceleration
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm
      - name: Actual v7 to v8 installation, data preservation and native role tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 36
          target: google_apis
          arch: x86_64
          profile: pixel_7
          disable-animations: true
          script: |
            bash scripts/test-android-upgrade.sh
            adb shell am start -W -n com.ewaste.marketplace.collector/com.ewaste.nativeapp.MainActivity
            adb shell uiautomator dump /sdcard/release-signin.xml
            adb pull /sdcard/release-signin.xml apk-download/release-signin.xml
            grep -q 'Sign up' apk-download/release-signin.xml
            adb exec-out screencap -p > apk-download/native-signin.png
            cd native-android && bash ./gradlew connectedDebugAndroidTest -PaccountFoundation=true -PinvitationSignIn=false -PaccountApiOrigin="$API_ORIGIN" -PappVersionCode=8 -Pandroid.testInstrumentationRunnerArguments.class=com.ewaste.nativeapp.AccountFoundationTest,com.ewaste.nativeapp.MarketplaceTest,com.ewaste.nativeapp.LogisticsTest,com.ewaste.nativeapp.FinanceTest,com.ewaste.nativeapp.NativeDesignTest,com.ewaste.nativeapp.AccountEntryTest,com.ewaste.nativeapp.FacilityTest --no-daemon
            adb pull /sdcard/ui-home.png apk-download/ui-home.png
            adb pull /sdcard/ui-photos.png apk-download/ui-photos.png
            adb pull /sdcard/ui-details.png apk-download/ui-details.png
            adb pull /sdcard/ui-earnings.png apk-download/ui-earnings.png
            adb pull /sdcard/ui-welcome.png apk-download/ui-welcome.png
            adb pull /sdcard/ui-roles.png apk-download/ui-roles.png
            adb pull /sdcard/ui-signup.png apk-download/ui-signup.png
            adb pull /sdcard/ui-login.png apk-download/ui-login.png
            adb pull /sdcard/ui-recycler-home.png apk-download/ui-recycler-home.png
            adb pull /sdcard/ui-facility-business.png apk-download/ui-facility-business.png
            adb pull /sdcard/ui-facility-services.png apk-download/ui-facility-services.png
            adb pull /sdcard/ui-facility-documents.png apk-download/ui-facility-documents.png
            adb pull /sdcard/ui-facility-pending.png apk-download/ui-facility-pending.png
            adb shell wm size 1080x1920
            adb shell wm density 480
            adb shell settings put system font_scale 1.3
            adb install -r native-android/app/build/outputs/apk/debug/app-debug.apk
            adb install -r -t native-android/app/build/outputs/apk/androidTest/debug/app-debug-androidTest.apk
            adb shell am instrument -w -r -e class com.ewaste.nativeapp.NativeDesignTest,com.ewaste.nativeapp.FacilityTest -e screenshotPrefix compact com.ewaste.marketplace.collector.test/androidx.test.runner.AndroidJUnitRunner | tee apk-download/compact-ui-test.txt
            grep -q 'OK (2 tests)' apk-download/compact-ui-test.txt
            adb pull /sdcard/compact-photos.png apk-download/compact-photos.png
            adb pull /sdcard/compact-earnings.png apk-download/compact-earnings.png
            adb pull /sdcard/compact-facility-business.png apk-download/compact-facility-business.png
            adb pull /sdcard/compact-facility-documents.png apk-download/compact-facility-documents.png
            adb shell wm size reset
            adb shell wm density reset
            adb shell settings put system font_scale 1.0
      - uses: actions/upload-artifact@v4
        with:
          name: E-Waste-Marketplace-native-v8
          path: apk-download/
          if-no-files-found: error
      - name: Publish a direct APK download
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create native-v8-demo apk-download/E-Waste-Marketplace-native-v8.apk apk-download/verification.txt apk-download/native-signin.png apk-download/ui-home.png apk-download/ui-photos.png apk-download/ui-earnings.png apk-download/ui-welcome.png apk-download/ui-roles.png apk-download/ui-signup.png apk-download/ui-login.png apk-download/ui-recycler-home.png apk-download/ui-facility-business.png apk-download/ui-facility-services.png apk-download/ui-facility-documents.png apk-download/ui-facility-pending.png --repo "$GITHUB_REPOSITORY" --target "$GITHUB_SHA" --title "E-Waste Marketplace — native accounts demo v8" --notes-file docs/native-v8-release.md --prerelease
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: native-v8-diagnostics
          path: |
            native-android/app/build/reports/androidTests/
            native-android/app/build/outputs/androidTest-results/
            upgrade-baseline/*.txt
      - name: Remove temporary signing material
        if: always()
        shell: bash
        run: |
          if [ -n "${EWASTE_SIGNING_STORE:-}" ]; then rm -f -- "$EWASTE_SIGNING_STORE"; fi
